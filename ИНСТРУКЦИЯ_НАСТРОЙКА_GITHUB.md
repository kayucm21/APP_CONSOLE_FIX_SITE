# 📋 ИНСТРУКЦИЯ ПО НАСТРОЙКЕ ПРОВЕРКИ ОБНОВЛЕНИЙ ОТ GITHUB

## 🎯 Информация о проекте

**Репозиторий GitHub:** https://github.com/kayucm21/APP_CONSOLE_FIX_SITE

**Название проекта:** APP_CONSOLE_FIX_SITE (ZAPRET FIX RUNET)

**Владелец репозитория:** kayucm21

---

## ✅ ВАЖНО: Git НЕ требуется!

**Программа проверяет обновления от GitHub автоматически БЕЗ установки Git!**

Программа использует встроенные средства Windows (PowerShell и GitHub API) для проверки наличия новых релизов на GitHub. **Установка Git НЕ нужна!**

---

## 🚀 Быстрый старт

### Шаг 1: Создайте репозиторий на GitHub (если еще нет)

1. Перейдите на [https://github.com](https://github.com)
2. Нажмите кнопку **"+"** в правом верхнем углу
3. Выберите **"New repository"**
4. Заполните данные:
   - **Repository name:** `APP_CONSOLE_FIX_SITE` (или другое название)
   - **Description:** `ZAPRET FIX RUNET - Обход блокировок интернета`
   - Выберите **Public** или **Private**
   - **НЕ** ставьте галочки на инициализацию
5. Нажмите **"Create repository"**

### Шаг 2: Настройка проверки обновлений в программе

✅ **УЖЕ НАСТРОЕНО!** Ссылка на репозиторий уже обновлена в коде:
- Репозиторий: https://github.com/kayucm21/APP_CONSOLE_FIX_SITE
- В коде (строка ~2968): `CheckGitHubUpdate("kayucm21", "APP_CONSOLE_FIX_SITE", true);`

Если нужно изменить на другой репозиторий:
1. Откройте файл `goodboyDPI.cpp` в редакторе кода
2. Найдите строку (около строки 2968):
   ```cpp
   CheckGitHubUpdate("kayucm21", "APP_CONSOLE_FIX_SITE", true);
   ```
3. Замените на ваши данные:
   ```cpp
   CheckGitHubUpdate("ваш_username", "имя_репозитория", true);
   ```

4. Сохраните файл и перекомпилируйте проект

---

## 🔄 Как работает проверка обновлений

### Автоматическая проверка при старте

1. Программа проверяет наличие интернета при запуске
2. Если интернет есть, автоматически проверяет наличие новых релизов на GitHub
3. Выводит информацию о последней доступной версии
4. **Работает БЕЗ установки Git!**

### Требования для проверки обновлений

- ✅ Интернет соединение
- ✅ Репозиторий на GitHub с релизами (releases)
- ✅ Правильно настроены параметры `repoOwner` и `repoName` в коде

### Как создать релиз на GitHub

1. Перейдите на страницу вашего репозитория на GitHub
2. Нажмите **"Releases"** (справа в меню)
3. Нажмите **"Create a new release"**
4. Заполните:
   - **Tag version:** например `v1.0.0`
   - **Release title:** например `Версия 1.0.0`
   - **Description:** описание изменений
5. Нажмите **"Publish release"**

Программа будет автоматически проверять наличие новых релизов при каждом запуске!

---

## 📝 Как обновить настройки в коде

### Файл: `goodboyDPI.cpp`

Текущая настройка в функции `main()`:
```cpp
CheckGitHubUpdate("kayucm21", "APP_CONSOLE_FIX_SITE", true);
```

Это соответствует репозиторию: https://github.com/kayucm21/APP_CONSOLE_FIX_SITE

Если нужно изменить на другой репозиторий, замените на:
```cpp
CheckGitHubUpdate("ваш_github_username", "имя_репозитория", true);
```

**Примеры:**

```cpp
// Текущий репозиторий: https://github.com/kayucm21/APP_CONSOLE_FIX_SITE
CheckGitHubUpdate("kayucm21", "APP_CONSOLE_FIX_SITE", true);

// Если другой репозиторий: https://github.com/IvanPetrov/APP_CONSOLE_FIX_SITE
CheckGitHubUpdate("IvanPetrov", "APP_CONSOLE_FIX_SITE", true);
```

---

## 🔧 Технические детали

### Как работает проверка

1. Программа использует **GitHub API** для получения информации о последнем релизе
2. Используется **PowerShell** с командой `Invoke-RestMethod` для HTTP запроса
3. URL формируется автоматически: `https://api.github.com/repos/{owner}/{repo}/releases/latest`
4. Получается тег версии последнего релиза (например, `v1.0.0`)
5. Информация выводится пользователю при запуске

### API GitHub

- **URL API:** `https://api.github.com/repos/{owner}/{repo}/releases/latest`
- **Метод:** GET
- **Не требует авторизации** для публичных репозиториев

---

## ❓ Часто задаваемые вопросы

### Вопрос: Нужно ли устанавливать Git?

**Ответ:** **НЕТ!** Git НЕ требуется. Программа использует только GitHub API через PowerShell.

### Вопрос: Почему не показывается версия?

**Ответ:** 
- Проверьте, есть ли интернет соединение
- Убедитесь, что правильно указаны `repoOwner` и `repoName` в коде
- Проверьте, что на GitHub есть хотя бы один релиз (release)

### Вопрос: Как отключить проверку обновлений?

**Ответ:** Закомментируйте строку в `main()`:
```cpp
// CheckGitHubUpdate("kayucm21", "APP_CONSOLE_FIX_SITE", true);
```

Или установите в `config.json`:
```
auto_update=false
```

### Вопрос: Программа показывает ошибку при проверке обновлений

**Ответ:** 
- Проверьте подключение к интернету
- Убедитесь, что репозиторий существует и доступен
- Проверьте правильность имени пользователя и репозитория
- Убедитесь, что на GitHub есть хотя бы один релиз

---

## 🔐 Безопасность

### Публичные репозитории

- Проверка обновлений работает для **публичных** репозиториев **без авторизации**
- Только чтение информации о релизах
- Не передаются никакие личные данные

### Приватные репозитории

Для приватных репозиториев нужна авторизация. Это можно добавить позже, если потребуется.

---

## ✅ Чеклист настройки

- [ ] Создан репозиторий на GitHub
- [ ] Создан хотя бы один релиз (release) на GitHub
- [ ] В коде `goodboyDPI.cpp` указан правильный `repoOwner` (имя пользователя GitHub)
- [ ] В коде `goodboyDPI.cpp` указан правильный `repoName` (имя репозитория)
- [ ] Проект перекомпилирован после изменений
- [ ] Проверено, что при запуске программы показывается версия с GitHub

---

## 📚 Полезные ссылки

- **GitHub:** [https://github.com](https://github.com)
- **GitHub API документация:** [https://docs.github.com/en/rest](https://docs.github.com/en/rest)
- **Создание релизов:** [https://docs.github.com/en/repositories/releasing-projects-on-github](https://docs.github.com/en/repositories/releasing-projects-on-github)

---

## 🎉 Готово!

Теперь программа автоматически проверяет наличие новых версий на GitHub при каждом запуске (если есть интернет)!

**Важно:** 
- ✅ **Git НЕ требуется!**
- ✅ Работает автоматически при наличии интернета
- ✅ Не требует дополнительной настройки после первоначальной настройки в коде

---

## 📝 Текущая настройка

✅ **УЖЕ НАСТРОЕНО!**

1. **Репозиторий на GitHub:**
   - Имя: `APP_CONSOLE_FIX_SITE`
   - Пользователь: `kayucm21`
   - Ссылка: https://github.com/kayucm21/APP_CONSOLE_FIX_SITE

2. **Настройка в коде:**
   ```cpp
   CheckGitHubUpdate("kayucm21", "APP_CONSOLE_FIX_SITE", true);
   ```

3. **Для работы проверки обновлений:**
   - Создайте релиз (release) на GitHub
   - Tag: например `v1.0.0`
   - Название: например `Версия 1.0.0`

4. **Запустите программу** - она автоматически покажет версию с GitHub!

**Важно:** Для проверки обновлений необходимо создать хотя бы один релиз (release) на GitHub!

**Успешной работы! 🚀**
